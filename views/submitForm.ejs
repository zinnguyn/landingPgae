<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Thông tin đăng ký - LadiGrowth</title>

    <!-- FontAwesome CDN for icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />

    <style>
      @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap");

      body {
        margin: 0;
        font-family: "Poppins", sans-serif;
        background: linear-gradient(to right, #470e53, #7f00ff);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        color: #fff;
        padding: 1rem;
      }

      .container {
        background: rgba(255 255 255 / 0.1);
        padding: 2.5rem 3rem;
        border-radius: 20px;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
        max-width: 420px;
        text-align: center;
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        position: relative;
      }

      .icon-success {
        font-size: 4rem;
        color: #00e676;
        margin-bottom: 1rem;
        animation: bounceIn 0.7s ease forwards;
      }

      @keyframes bounceIn {
        0% {
          transform: scale(0.3);
          opacity: 0;
        }
        60% {
          transform: scale(1.1);
          opacity: 1;
        }
        80% {
          transform: scale(0.9);
        }
        100% {
          transform: scale(1);
        }
      }

      h1 {
        font-weight: 700;
        margin-bottom: 1rem;
        font-size: 2rem;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        text-shadow: 0 2px 6px rgba(0, 0, 0, 0.3);
      }

      p {
        font-weight: 400;
        margin-bottom: 0.75rem;
        font-size: 1.15rem;
      }

      .info-label {
        font-weight: 700;
        margin-right: 0.5rem;
        color: #ffd6ff;
        display: inline-flex;
        align-items: center;
      }

      .info-label i {
        margin-right: 6px;
        color: #ff7bff;
        font-size: 1.1rem;
      }

      .btn-home {
        margin-top: 2.5rem;
        background: #9c27b0;
        color: white;
        padding: 0.85rem 2rem;
        border: none;
        border-radius: 30px;
        font-weight: 700;
        font-size: 1.1rem;
        cursor: pointer;
        transition: background 0.3s ease;
        text-decoration: none;
        display: inline-block;
        box-shadow: 0 6px 15px rgba(156, 39, 176, 0.5);
      }

      .btn-home:hover {
        background: #7a1a90;
        box-shadow: 0 8px 25px rgba(122, 26, 144, 0.7);
      }
    </style>
  </head>

  <body>
    <div class="container">
      <i class="fas fa-check-circle icon-success"></i>
      <h1>Cảm ơn bạn đã đăng ký!</h1>
      <p>Chúng tôi đã nhận được thông tin của bạn:</p>

      <p>
        <span class="info-label"><i class="fas fa-user"></i>Họ và Tên:</span><%=
        name %>
      </p>
      <p>
        <span class="info-label"
          ><i class="fas fa-phone"></i>Số điện thoại:</span
        ><%= phone %>
      </p>
      <p>
        <span class="info-label"
          ><i class="fas fa-palette"></i>Gói thiết kế:</span
        ><%= designType %>
      </p>
      <p>
        <span class="info-label"
          ><i class="fas fa-comment-dots"></i>Lời nhắn:</span
        ><%= message || "Không có" %>
      </p>

      <a href="/" class="btn-home">Quay về trang chủ</a>
    </div>
  </body>
</html>
